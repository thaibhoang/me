<header>
  <%= content_tag heading_tag, title, class: heading_classes %>
  <% if subtitle.present? %>
    <p class="mt-3 max-w-3xl text-sm leading-relaxed text-slate-400 sm:text-base"><%= subtitle %></p>
  <% end %>
</header>
